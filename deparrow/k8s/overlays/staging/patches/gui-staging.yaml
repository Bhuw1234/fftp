# GUI staging resources
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gui
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: gui
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
